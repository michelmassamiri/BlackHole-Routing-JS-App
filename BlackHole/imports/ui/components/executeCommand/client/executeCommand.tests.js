import { Template } from 'meteor/templating';
import '../executeCommand.js';

describe('ExecuteCommand', function () {
 it('Execute shutdown command with success', function () {

 });

 it('Execute reload command with success', function () {

 });

 it('Execute restart command with success', function () {

 });

 it('Should not execute any other command than shutdown, restart, reload', function () {

 });

 it('Should not execute command if ExaBGP is not running', function () {

 });
});