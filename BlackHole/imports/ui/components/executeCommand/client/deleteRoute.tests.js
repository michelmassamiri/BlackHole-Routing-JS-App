import { Template } from 'meteor/templating';
import '../deleteRoute/deleteRoute.js';

describe('Delete_Route: withdraw command (ExaBGP)', function () {
   it('Should not fire HTTP requests if IP source is not valid', function () {

   });

   it('Fire HTTP request if IP source is valid and IP source is the only input', function () {

   });

   it('Should not fire HTTP requests if next-hop is not valid', function () {

   });

   it('Should fire HTTP requests if IP source, next-hop are valid', function () {

   });
});