import { Template } from 'meteor/templating';
import '../showNeighbors/showNeighbors.js';

describe('Show_Neighbors_summary : show neighbors summary ExaBGP command', function () {
   it('Should not fire HTTP request if neighbor ip input was provided and not valid', function () {

   });

   it('Fire HTTP requests and print out the neighbors in the return-message div', function () {

   });
});