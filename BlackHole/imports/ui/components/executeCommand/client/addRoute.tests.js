import { Template } from 'meteor/templating';
import '../addRoute/addRoute.js';

describe('Add_Route', function () {
   it('Should not fire HTTP request if IP source input is not valid', function () {

   });

   it('Shoud not fire HTTP request if next-hop input is not valid', function () {

   });

   it('Should fire HTTP request if IP source and next-hop were valid inputs', function () {

   });

   it('Fire HTTP request if IP source, next-hop, community, local-preference were provided', function () {

   });

});